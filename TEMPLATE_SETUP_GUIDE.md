# 🔧 คำแนะนำการตั้งค่า Email Template

## ⚠️ ปัญหาที่พบบ่อย: อีเมลไม่ส่ง

ถ้าอีเมลไม่ส่งมา แม้ว่า Service ID, Template ID และ Public Key จะถูกต้องแล้ว ปัญหาน่าจะอยู่ที่ **Email Template**

## ✅ วิธีตั้งค่า Template ให้ถูกต้อง

### 1. ไปที่ Email Templates ใน EmailJS Dashboard
- เปิด Template ID: `template_q0d7bey`
- หรือ Template ที่ชื่อ "Contact Us" (ตามรูปที่คุณส่งมา)

### 2. ตั้งค่าในช่อง "To Email" (สำคัญมาก!)
```
{{to_email}}
```
⚠️ **ต้องใส่ `{{to_email}}` ในช่องนี้** ไม่ใช่ใส่อีเมลจริง

### 3. ตั้งค่า Subject (หัวข้ออีเมล)
```
แจ้งเตือน: {{event_title}}
```
หรือ
```
การแจ้งเตือนจากปฏิทิน M.5
```

### 4. ตั้งค่า Content (เนื้อหาอีเมล)

**แบบ Text:**
```
สวัสดีครับ/ค่ะ

นี่คือการแจ้งเตือนจากปฏิทิน M.5

ชื่องาน: {{event_title}}
วันที่/เวลา: {{event_datetime}}
รายละเอียด: {{event_note}}

ขอบคุณที่ใช้บริการ
```

**แบบ HTML:**
```html
<p>สวัสดีครับ/ค่ะ</p>
<p>นี่คือการแจ้งเตือนจากปฏิทิน M.5</p>
<br>
<p><strong>ชื่องาน:</strong> {{event_title}}</p>
<p><strong>วันที่/เวลา:</strong> {{event_datetime}}</p>
<p><strong>รายละเอียด:</strong> {{event_note}}</p>
<br>
<p>ขอบคุณที่ใช้บริการ</p>
```

## 📋 ตัวแปรที่ใช้ได้

โค้ดจะส่งตัวแปรเหล่านี้ไปให้ Template:

- `{{to_email}}` - อีเมลผู้รับ (ต้องใส่ในช่อง "To Email")
- `{{event_title}}` - ชื่องาน
- `{{event_note}}` - รายละเอียดงาน
- `{{event_datetime}}` - วันที่/เวลาแบบเต็ม
- `{{event_date}}` - วันที่แบบสั้น

## 🔍 วิธีตรวจสอบ

1. เปิด Console ในเบราว์เซอร์ (กด F12)
2. คลิกปุ่ม "ทดสอบส่งอีเมล"
3. ดู Console:
   - ถ้าเห็น "✅ ส่งอีเมลแจ้งเตือนสำเร็จ!" = ส่งสำเร็จ
   - ถ้าเห็น "❌" = มีปัญหา ดู error message

## ⚠️ ข้อผิดพลาดที่พบบ่อย

### 1. ไม่ใส่ `{{to_email}}` ในช่อง "To Email"
- ❌ ผิด: ใส่อีเมลจริง เช่น `user@example.com`
- ✅ ถูก: ใส่ `{{to_email}}`

### 2. ตัวแปรชื่อไม่ตรง
- ❌ ผิด: `{{title}}`, `{{note}}`
- ✅ ถูก: `{{event_title}}`, `{{event_note}}`

### 3. Template ไม่ได้ Save
- ต้องคลิก "Save" หลังจากแก้ไข

## 🧪 ทดสอบการส่ง

1. เปิด `index.html` ในเบราว์เซอร์
2. กรอกอีเมลของคุณ
3. คลิกปุ่ม "ทดสอบส่งอีเมล"
4. ตรวจสอบ Console (F12) ว่ามี error หรือไม่
5. ตรวจสอบกล่องจดหมาย (รวมถึง Spam/Junk)

## 📞 ถ้ายังไม่ส่ง

1. ตรวจสอบ Console (F12) ดู error message
2. ตรวจสอบว่า Gmail Service เชื่อมต่อแล้ว
3. ตรวจสอบว่าไม่เกินโควต้า (200 อีเมล/เดือน)
4. ลองสร้าง Template ใหม่แล้วตั้งค่าใหม่

